# uisight — hafıza arşivi

Bu dosya, merkezi hafızadaki `reference_mobil_qa_araci` notunun taşan tarihçesidir
(kural: topic dosyası 3KB'yi aşmaz, taşan tarihçe repoya gider). Merkezi hafızada
yalnız güncel gerçek ve buraya işaret kalır.

Aşağısı 9 Ağustos – 4 Eylül 2026 arasında biriken kayıt; tarih sırasıyla değil
konu bloklarıyla yazılmıştı.

**Tarihî belge — bazı satırlar artık geçerli değil.** En belirgini:
`AGENTS/tools/mobil-qa` çatallanması 4 Eylül 2026'da emekliye ayrıldı, kod tek
yerde toplandı (`c:/dev/uisight`), IDE eklentisi ve MCP kaydı oraya çevrildi.
Güncel durum için repo kökündeki `README.md` ve `CHANGELOG.md` esastır.

---

Telefonda ekran görüntüsü alıp sohbete yapıştırma derdini bitiren set (9 Ağu 2026 kuruldu).

🚀 **F2-LITE YAYINDA (17 Ağu gecesi):** ürün adı **uisight** · GitHub PUBLIC: `github.com/sololabstr/uisight` (MIT) · `c:\dev\uisight` ayrı repo. Dış yüzey EN (MCP: see_screen/inspect/goto/tap/type_text/scroll/set_device/status/marks; `UISIGHT_LANG=tr` ile TR alias), iç kod TR (v0.2 borcu, README'de şeffaf). CLI çıktısı kullanıcı cwd'ine (`uisight-outputs/`), panel dosyaları `~/.uisight/live` (npx güvenliği). Bizim AGENTS kopyası AYRI yaşıyor (TR, kök kural ona bağlı) — bilinçli çatal, tam F2'de birleşir. 🔒 **GÜVENLİK DENETİMİ 20 Ağu (0.1.3 CANLI — npm + registry + GitHub üçü de eşit):** 3 paralel ajan (2'si bağımsız güvenlik) 16 bulgu; 4 KRİTİK/YÜKSEK gerçek istekle ölçülerek kapatıldı → **sunucu 0.0.0.0'a bind ediyordu** (LAN'daki herkes oturumu sürebilirdi) → 127.0.0.1 · **/eylem CSRF'e açıktı** (text/plain hilesi preflight'ı atlar) → per-run token + Host allowlist · **oturum id'si dosya yoluna gidiyordu** (path traversal, her karede tetiklenir) → allowlist · **panel denetlenen sayfanın metnini innerHTML'e basıyordu** (stored XSS, panel origin'inde) → escape. Ayrıca: REPORT.md kendi bulgusunun altına 'temiz' yazıyordu · MCP ölen paneli bir daha başlatmıyordu · panel açma macOS/Linux'ta SESSİZCE ölüyordu · shell'siz spawn · isError tüm araçlarda · paralel başlatma · 1MB gövde sınırı. Rapor: repo `docs/AUDIT_2026_08_20.md`. 🔑 DERS: hızlı yazılan yerel-sunucu aracında varsayılanlar (host'suz listen, origin'siz POST) sessiz saldırı yüzeyi açar; **yayınlanan sürüm git'ten sapabilir** (0.1.2'de 3 düzeltme commit'liydi ama publish edilmemişti). 🔴 AÇIK: test YOK · CI YOK (ikisi de raporda).

✅ **npm CANLI (18 Ağu 00:26): `uisight@0.1.0`** — hesap `sololabs` (2FA passkey; recovery Bitwarden'da), yayın kullanıcı terminalinden (2FA'lı publish web-onay ister, headless kabuktan OLMAZ — gelecek yayınlar da kullanıcı terminali ya da granular token). `npx uisight` temiz-makine testi geçti. ✅ **RESMİ MCP REGISTRY KAYITLI (19 Ağu 23:33): `io.github.yusufcemres/uisight` v0.1.2** — Claude/Cursor katalogları buradan besleniyor. Kayıt yolu: paket `mcpName` + `server.json` + `mcp-publisher login github` (cihaz kodu; JWT DAKİKALAR içinde ölür → login+publish tek zincirde). 🔑 ORG NAMESPACE BUG'I: `io.github.sololabstr/*` izni public-üyelik + OAuth-kısıt-kaldırma yapılsa da GELMEDİ (registry hep `io.github.yusufcemres/*` verdi) → kişisel namespace'e pivot; org çözülünce yeniden kayıt + eski deprecate. npm 2FA yayın dersi: HER publish tarayıcı onayı ister, headless kabuktan URL maskeli (***) basılır → publish DAİMA kullanıcı terminalinden. ✅ registry org-bug issue AÇILDI: modelcontextprotocol/registry#1551 (çözülünce io.github.sololabstr'a geçiş). ✅ Lansman içeriği HAZIR (TASLAK, onay bekler): `AGENTS/tools/mobil-qa/docs/DUYURU_ICERIK_2026_08_19.md` — LinkedIn TR + X TR/EN + Show HN + **WhatsApp AI-grup mesajları (A kısa · B teknik/MCP · C tek-satır)**. 🔴 Paylaşımda İNDİRME SAYISI VERME (npm ilk hafta ~bot/ayna trafiği). 🔴 KULLANICI (2'şer dk, tarayıcı): Smithery'de GitHub-login ile server ekleme (smithery.ai) · mcp.so submit formu (bot-duvarlı, insan gerekli).

🚀 **X PATLAMASI + F2 KAPISI AÇILDI (25-30 Ağu):** Erdal Bektaş (@eeebektas) X'te paylaştı → 28 B görüntülenme · 341 beğeni · 4 günde **yıldız 2→99 · fork 7 · tekil klon 42 · npm gerçek indirme ~118** (istikrarlı, bot deseni değil). Kanıt: gerçek insanlar kullanıyor. 🔑 **EN DEĞERLİ ÇIKTI konumlandırma dersiydi:** 19 yorumun ~7'si *"computer use/browser use zaten yapıyor, kazanç ne"* dedi ve HAKLIYDILAR — mesajımız "AI ekranı göremiyor" diyordu, oysa AI görüyor; iddiamız hep "görmek ≠ ölçmek"ti ama ilk satırda söylenmiyordu. **0.1.4'te düzeltildi:** başlık *"can already see — just can't measure"*, açılışta tahmin-vs-ölçüm diff'i, "My agent already does this" bölümü (4.6:1 ile 4.3:1 gözle AYNI görünür · token · ortak panel), kapsam en başta (web/responsive), tablo 4 sütun. **Ders: tekrar eden itiraz = konumlandırma raporu; ürün doğruyken ilk cümle yanlış olabilir.** Rakip haritası netleşti: Argent (2.286★, native iOS/Android, ölçüm YOK — README'de kredilendirildi) · Polypane (insana ölçer) · Playwright MCP/harness'lar (ölçmez). ⚠️ 42 klon/7 fork'a rağmen **0 issue** → takılan sessizce vazgeçiyor; issue daveti + bilinen sınırlar README'de görünür olmalı. ✅ **TEST + CI KURULDU (30 Ağu):** 13 regresyon testi (`test/inspect.test.mjs`) — hepsi motorun GERÇEKTEN yaptığı hatalar: yanlış-temiz (oklab gradient, div içi metin) · yanlış-alarm (alfa-harmanlı katman, ikon-font, foto zemin) · WCAG AA sınırı (4,54 geçer / 3,95 kalır). 🔑 **Matematik ayrı modüle ÇIKARILMADI** — `DENETIM_KODU` page.evaluate ile serileşiyor, dış scope'a erişemiyor; çıkarmak = ikinci kopya = kopya çürür (kalibrasyon #13). Testler gerçek fonksiyonu gerçek tarayıcıda, elle hesaplanmış cevaplarla sürüyor. CI (GitHub Actions): syntax + test + `npm pack --dry-run` + **package.json↔server.json sürüm senkron kapısı** (0.1.2'de yaşanan yayın-sapmasını yakalar). 🔑 **CI ilk koşusunda kendini kanıtladı:** `node --test "glob"` yerelde Node 24'te çalışıyordu, CI'da Node 20'de literal dosya adı sanıldı → argümansız `node --test`. "Yerelde geçti" kanıt değil. ✅ Issue şablonu + README'de açık davet + bilinen sınırlar (sınır ≠ bug ayrımı).
🌐 **İÇ KOD EN GÖÇÜ TAMAM — 0.2.0 (30 Ağu, PR #2):** `src/` + `test/` baştan sona İngilizce (tanımlayıcı, yorum, HTTP ucu, JSON alanı). Kıran değişiklik: `/eylem→/action` `/akis→/stream` `/kare→/frame` `/durum→/state` `/isaretler→/marks`; `tip→type` ve eylem adları (`git→goto` `tikla→click` `kaydir→scroll` `tus→press` `denetle→inspect` `kaydet→save`); `?tam→?full` `?temizle→?clear`; `secici→sel` `kenar→border` `konsol/ag→console/network` `kod→status`. CLI bayrakları, 9 MCP aracı ve rapor biçimi DEĞİŞMEDİ. Tablo: repo `CHANGELOG.md`. Yeni: `CONTRIBUTING.md` (motor matematiği neden modüle çıkarılamaz) + `smithery.yaml`.
🔑 **GÖÇÜN İKİ DERSİ (ikisi de gerçek hasardan):** ① İlk deneme kodu BOZDU ve geri alındı — toptan `not→note` İngilizce düzyazıyı da çevirdi ("could note open browser"), `mobil→mobile` ise `SESSION_MAP` ve enum'da ÇİFT ANAHTAR yarattı. Syntax kontrolü ikisini de geçti. Ders: **İngilizceyle çakışan kısa kelime toptan haritaya girmez, bağlamla ele alınır.** ② İkinci deneme çift dilli araç adı tablosuna da girdi: `UISIGHT_LANG=tr` yarısı İngilizce araçlar (`inspect`, `goto`, `click`) yayınlıyordu. 13 denetim testi, syntax kapısı ve CI'nın hepsi geçti — **araç ADI ajanın bağlandığı sözleşme ve hiçbir test ona bakmıyordu.** Artık bakıyor: `test/mcp-tools.test.mjs` (4 test, gerçek stdio, panel gerekmez). O testin ilk koşusu göç artığı değil GERÇEK bir eksik buldu: `tap.x/y`, `type_text.text`, `set_device.theme` hiç açıklama taşımıyordu (ajan tahmin ediyordu) — dolduruldu. Toplam **17 test**.
✅ Davranış kanıtı varsayılmadı ölçüldü: göç ÖNCESİ altın kopya alındı (nfc.sololabs.tr, 6 cihaz×tema), sonrası birebir aynı (44px 1/1/0 · 12px 4 · imza 25 · altsız 2 · tema-donuk yok). Panel canlı: 9 eylem ok, `/frame` 22KB & full=1 263KB, `/marks` not+görsel gidiş-dönüş, tokensiz POST hâlâ 403, yabancı Host hâlâ ret. MCP stdio: 9 araç.
🔀 **İÇ FORK (`AGENTS/tools/mobil-qa`) DURUMU ÖLÇÜLDÜ:** motor mantığı 14 ayırt edici izin hepsinde uisight ile AYNI (sapma yok). Araç adları da artık birebir aynı 9 ad; TEK fark uisight'ın parametre anahtarlarının İngilizce olması (`oturum→session`, `secici→selector`, `temizle→clear`) — ajan şemayı çağrı anında okuduğu için görünmez. **Yani fork emekliye ayrılabilir: kayıtlı `mobil-qa` MCP'si `uisight/src/mcp.mjs` + `UISIGHT_LANG=tr`'ye çevrilir.** 🔴 AMA IDE eklentisi de forka çivili (`mobilQa.aracYolu` varsayılanı + kökte `sunucu.mjs` bekliyor, uisight'ta `src/server.mjs`) → tam birleşme = eklenti kodu + .vsix yeniden paket + 2 IDE'ye kurulum. Yarım birleşme (MCP taşınır, eklenti kalır) iki panel iki port demek — **ya hepsi ya hiçbiri.** Kazanç: bugün iki kod tabanı elle senkronlanıyor ve yalnız biri test+CI'lı.
📱 **EMÜLATÖR + CANLI UYGULAMA ÖLÇÜMÜ (4 Eyl gecesi):** Android SDK + emülatör ZATEN kuruluydu (`c:\devndroid-sdk`, AVD `fiko-test` = Pixel 7 / API 35) — hiçbir şey indirilmedi. 🔑 **adb forward tcp:9222 localabstract:chrome_devtools_remote + Playwright connectOverCDP** → uisight motoru emülatörde koşan GERÇEK TWA'da çalışıyor. Simülasyon değil: gerçek Android Chrome, ekran gerçek cihazdan (`adb screencap`). Yeni araç `AGENTS/tools/emu-qa/emu-qa.mjs`: kendi gezinir (rotaları uygulamanın bağlantılarından bulur — elle tahmin `/rehber-ara` 404'tü, gerçeği `/ara`) · `--isaret` tek düğmeyle hata yakalama (uisight işaret kuyruğuna düşer) · `--giris eposta:kod` girişli tarama · `--play` ekranı 2:1'e çeker. 🔑 **Play 1080×2400'ü REDDEDER** (uzun kenar ≤ 2× kısa kenar); emülatör ekranını `wm size 1080x1920` ile çekince kare doğrudan yüklenebilir. 🔑 Web katmanının YAPISAL olarak göremediği tek şey Android kabuğu: `TranslucentCustomTabActivity`=TWA sağlıklı · `CustomTabActivity`=adres çubuğu VAR, kabuktan düşülmüş · `FirstRunActivity`=Chrome karşılama ekranı önü kapatıyor. Antigravity CLI `_/bin/antigravity-ide.cmd` (eski yol BOŞ).
🧩 **ÜÇ YENİ KONTROL — motorun kör noktasıydı (uisight 0.3.0/0.3.1, fork'a da taşındı):** `coveredControls` (üstüne binen) · `coveredByFixed` (sabit çubuk altında) · `clippedText` (kırpılan yazı). Kullanıcının elle çektiği 4 telefon ekranındaki hataları motor "temiz" ölçmüştü; kontrast ve boyut kuralları bir öğenin başkasının üstünde olduğunu göremez. 🔑 **İLK YAZIM KÖRDÜ:** yalnız öğenin MERKEZİNE bakıyordu, FAB ise geniş CTA'nın SAĞ UCUNU kapatır — merkez boş kalır. Yapay kopyayla sınamasam "temiz" diyecekti. Izgara kenardan kenara örnekler. 🔴 **14 canlı sitede 36 bulgu → doğrulayınca 3 gerçek.** Dört yanlış-alarm sınıfı elendi, hepsi test: ① modal altındakini kapatır (perdeyi muaf tutmak YETMEZ, dialog'un içerik kutusu onun yerine geçer → atalara bak) ② `line-clamp` bilinçli kısaltma (18 kırpığın 17'si) ③ **geometri YALAN SÖYLER** — sabit öğe kesişip yine de ARKADA olabilir (redios: yüzen düğme çerez butonlarını geometrik kapatıyor ama arkasında, butonlar okunur) → `elementFromPoint`'e sor. 🔑 Ders: doğru davranışta öten kontrol, kimsenin dinlemediği kontroldür — yanlış-alarm testleri tespit testlerinden önemli. 30 test.
🔌 **ECLENTİ v1.1.0 — sol menüde kendi ikonu** (`viewsContainers.activitybar` + `WebviewViewProvider`); panel `?dar=1` ile tek sütun (kenar çubuğu ~300px, telefon çerçevesi 412px). İki IDE'ye de kurulu. Gerekçe kullanıcının: *"kod bilmeyenler kullanacak, ne kadar göz önünde olursa bilinirliği o kadar artar"* — keşfedilebilirlik konfor değil, ürünün kendisi.
🔴 SIRADA: **npm publish 0.3.1 KULLANICI TERMİNALİNDEN** (2FA tarayıcı onayı; ardından `mcp-publisher publish` ile registry) · Smithery/mcp.so listelemeleri · org namespace (registry#1551, 19 Ağu'dan beri 0 yorum) · fork birleştirme kararı.

📈 **F2 kanıt defteri:** 17 Ağu 23:10 — refleks kuralının yayılımından ~30 dk sonra İLK ORGANİK KULLANIM: NFC oturumu kendiliğinden `mobil` koştu ve kendi taramasının kaçırdığı 1.01:1 görünmez hero başlığını yakaladı (`ciktilar/nfc...17-20-10/`). Tez üçüncü kez doğrulandı, bu kez başka bir Claude üstünde.

**1) `mobil-qa` — otomatik emülasyon + denetim** (`c:\dev\AGENTS\tools\mobil-qa`, PATH'te)
- `mobil <url> --cihaz iphone-15,iphone-se,pixel,galaxy,ipad --tema ikisi --yol /,/menu --tam`
- Çıktı: `ciktilar/<host>-<zaman>/` → PNG + **`galeri.html`** + `RAPOR.md` + `rapor.json`. PNG'ler `scale:'css'` ile küçük → Claude doğrudan Read ile **görür**.
- **`galeri.html` = Android Studio Compose Preview panelinin karşılığı**: tüm cihaz×tema kartları yan yana, numaralı, bulgu rozetli; koşu sonunda tarayıcıda otomatik açılır (`--acma` ile kapatılır). Kullanıcı sorunu **kart numarasıyla** tarif eder ("3 numarada üst menü çakışıyor").
- `--izle <sn>`: sabit `ciktilar/_canli-<host>/` klasörüne çeker, panel meta-refresh ile kendini yeniler → localhost'ta kod değiştirdikçe canlı önizleme.
- 🔴 Compose `@Preview` yalnız Kotlin/native içindir; Next.js/PWA projelerinde çalışmaz — bu aracın var oluş nedeni.
- Otomatik yakaladıkları: yatay taşma · 44px altı dokunma hedefi · 12px altı yazı · görünmez metin (kontrast <1.6) · WCAG AA altı kontrast · buton sorunları · konsol/JS hatası · 4xx-5xx istek · **light↔dark tema karşılaştırması** (iki temada aynı kalan = sabit-kodlu renk).
- ✅ **WebKit 17 Ağu ONARILDI** — iPhone profilleri artık GERÇEK Safari motoruyla koşar (`rapor.json` motor:webkit ile doğrulandı). Onarım: yönetici PowerShell ile `%LOCALAPPDATA%\ms-playwright` Defender istisnası + WebKit yeniden indirme (Defender geçmişinde tehdit kaydı YOKTU — kök kesinleşmedi, istisna+taze indirme çözdü). Chromium-fallback kodu güvenlik ağı olarak DURUYOR. Not: Windows'ta iOS Simulator hâlâ YOK; WebKit ≈ Safari motoru, birebir iOS değil.

**2) CANLI PANEL — ÇOK OTURUMLU, insan+AI aynı ekranda** (`sunucu.mjs`; ✅ F1 DOĞRULANDI 9 Ağu gece)
- `node sunucu.mjs <url>` → **web (1440px) + mobil (Pixel) YAN YANA**, ikisi de canlı; `--tek` yalnız mobil, `--cihaz/--web` profil seçer. Adres çubuğu TÜM oturumlara gider (URL-senkron); tıklama/yazma tıklanan ekrana; her pane'de profil seçici + **📌 İşaretle** (not+kare → `canli/isaretler/` kuyruğu = insan→AI kanalı).
- Kare akışı: CDP screencast, 3 denemede açılmazsa saniyede-bir görüntüye düşer (sunucu ÖLMEZ; `MOBILQA_YEDEK=1` yedek yolu zorlar — test edildi). EADDRINUSE net mesajla çıkar. "Denetle" tüm oturumlarda, bulgular cihaz etiketli.
- Denetim kuralları profile duyarlı: 44px dokunma hedefi yalnız mobil profillerde; `masaustu`(1440)/`laptop`(1366) profilleri CLI'da da geçerli.

**2a) MCP SUNUCUSU = ÜRÜN OMURGASI** (`mcp.mjs`; ✅ Claude Code user-scope KAYITLI; **17 Ağu oturum-içi CANLI DOĞRULANDI**: durum→git→ekrani_gor→denetle zinciri uçtan uca; motor otomatik başlatma çalıştı. Aynı testte kurulum-goto yarışı bulundu ve kapatıldı: oturum Map'e sayfa oturunca girer, MCP "hazır"=oturum≥1. **F2 kapısı 24 Ağu'a uzatıldı — ölçüt gerçek işlerde kullanım (`ciktilar/` izleri kanıt); refleks kuralı [[feedback_ui_isinde_mobil_qa_denetimi]] + kök CLAUDE.md'de**)
- Araçlar: `ekrani_gor` (görüntü döner) · `denetle` (ÖLÇÜM metin olarak — token tasarrufunun kalbi) · `git/tikla(secici|x,y)/yaz/kaydir` · `cihaz_degistir` · `durum` (son konsol/ağ/işaret kayıtları) · `isaretler` (kullanıcının 📌 notları + kare; okununca kuyruktan düşer).
- Panel ayakta değilse MCP kendisi başlatır (`MOBILQA_URL` vars. localhost:3000, `MOBILQA_PORT` vars. 5055). Kayıt: `claude mcp add --scope user mobil-qa -- node c:\dev\AGENTS\tools\mobil-qa\mcp.mjs`.
- Artık dosya köprüsü (`canli/son.jpg`) GEREKMEZ — AI araç çağırır. Karar: MCP=omurga, eklenti=opsiyonel konfor (pazar analizi: Polypane AI'sız, Playwright MCP ölçümsüz+panelsiz; kesişim boş).

**2b) IDE EKLENTİSİ — `sololabs.sololabs-mobil-qa` (OPSİYONEL konfor; omurga MCP)** (kaynak `eklenti/`, güncel paket v1.0.1 — spawn `shell:true` + `mobilQa.nodeYolu` ayarı + gerçek hata mesajı; ⚠️ IDE-içi soğuk açılış testi KULLANICIDA)
- **Antigravity IDE + VS Code ikisine de KURULU** (9 Ağu 2026). Komut paleti: "Mobil Panel: Aç / Cihaz seç / Temayı değiştir / Adresi değiştir / Denetle / Claude'a görüntü gönder / Durdur". Sağ altta durum çubuğu düğmesi.
- Sunucuyu kendi başlatır (zaten ayaktaysa ona bağlanır), paneli yan sekmede webview olarak açar (`portMapping`).
- Ayarlar: `mobilQa.url` · `.cihaz` · `.tema` · `.port` · `.aracYolu`. Playwright pakete GÖMÜLMEZ — `aracYolu` altındaki node_modules kullanılır (.vsix 6 KB).
- Yeniden paketleme: `cd eklenti && npx @vscode/vsce package --allow-missing-repository --skip-license`; kurulum: `antigravity-ide --install-extension <vsix> --force` (CLI: `%LOCALAPPDATA%\Programs\Antigravity IDE\bin\antigravity-ide.cmd`).

**3) `scrcpy` — gerçek telefon, bilgisayar ekranında** (winget `Genymobile.scrcpy`, PATH'te; `adb` yanında)
- USB + USB hata ayıklama → `scrcpy` ile ayna, fare/klavye ile kullan.
- Claude'un görmesi için: `adb exec-out screencap -p > <dosya>.png` → Read.
- NFC çip okuma, gerçek PWA kurulumu, bildirim yakalama gibi emülatörün yapamadıkları için tek gerçek yol.

**3) Android Studio Emulator** — KURULMADI (~10-15 GB). Sadece telefon yokken / farklı Android sürümü / TWA-APK testinde gerekli; iOS'u çözmez, otomatik değildir.

🔴 **YANLIŞ POZİTİF TURU** (9 Ağu gece, hesapla vakası — motor 2. tur, hepsi düzeltildi): yarı saydam zemin (`bg-white/15`) harmanlanmadan düz renk sanılıyordu → artık katmanlar **alfa-harmanlanır** · foto (`url()`) üstü metin ÖLÇÜLEMEZ → motor artık susar (yanlış alarm yerine) · Material Symbols ligature'ı ("restaurant") metin sanılıyordu → ikon fontlar muaf · range input `value`'su ve text-stroke kontur-yazı muaf. 12 yanlış pozitif → 0; gerçek bulgular (3.35:1 CTA, 2.89:1 butonlar, 44px) DURUYOR — iki yönlü doğrulandı.

🔴 **ÖLÇÜM YANLIŞ "TEMİZ" DİYEBİLİR** (9 Ağu, NFC vakası — üç ayrı kör nokta, üçü de düzeltildi):
1. Metin taraması sabit etiket listesiyle (`p,span,a,h1…`) sınırlıydı → `<div>` içindeki metin (nfc-card-3d) hiç ölçülmedi. Artık `body *` taranıp "kendi metin düğümü" filtresi uygulanıyor.
2. Arka plan hesabı yalnız `background-color` bakıyordu → gradient zeminli koyu kartın üstündeki metni **beyaz sayfa zemini** ile ölçüp kontrastı OLDUĞUNDAN İYİ gösterdi (1.14:1 gerçeği yerine "temiz"). Artık gradient durakları ortalanıyor.
3. Renk ayrıştırıcı yalnız `rgb/rgba` tanıyordu → Tailwind v4'ün `oklab()` gradient durakları sessizce boş geçti. Artık renk çözümlemesi canvas'a (tarayıcı motoruna) devredildi; oklab/oklch/color-mix hepsi çalışır.
🔑 Ders: "bulgu yok" ≠ "sorun yok". Yanlış temiz rapor, eksik rapordan tehlikeli — her yeni sitede gözle bir tur şart.

🔑 Ders: otomatik denetim tasarım hatasını görmez — iPhone SE (320px) navbar çakışmasını yalnız **görsel** yakaladı. Bkz [[feedback_uretilen_ciktiya_gozle_bak]].
